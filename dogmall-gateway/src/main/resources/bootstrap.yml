spring:
  application:
    name: dogmail-gateway
  cloud:
    nacos:
      config:
        file-extension: yaml
        name: dogmail-gateway
        group: dogmail
        namespace: ${NACOS_NAMESPACE:10001}
        server-addr: ${NACOS_SERVER_ADDR:192.168.111.132:8848}
      discovery:
        namespace: ${NACOS_NAMESPACE:10001}
        group: dogmail
        server-addr: ${NACOS_SERVER_ADDR:192.168.111.132:8848}
